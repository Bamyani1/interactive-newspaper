# Files whose exact source text is a contract asserted by
# tests/components/ui/design-tokens.test.ts. Prettier would reformat them
# into a passing-looking file that fails those assertions:
#   - colors.css must mirror design.md's palette byte-for-byte, including
#     its uppercase hex (prettier lowercases hex).
#   - the primitives gallery is pinned as a literal JSX string (prettier
#     reflows the attributes onto separate lines).
src/styles/tokens/colors.css
src/app/dev/primitives/page.tsx

# Generated or frozen data. Reformatting changes bytes that are either
# regenerated by a script (so the diff never settles) or hashed by the
# evaluation freeze checks.
scripts/db/schema-snapshot.json
tests/api/rag-golden-baseline.json
tests/api/rag-golden-questions.json

# Vendored third-party document (Google design.md v1, Apache-2.0).
design.md
