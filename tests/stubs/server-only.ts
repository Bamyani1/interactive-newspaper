// Stub for the `server-only` package used by Next.js to enforce
// server-only module boundaries. Vitest has no such boundary, so we
// resolve the import to this empty module and let the rest of the
// file run normally.
export {};
